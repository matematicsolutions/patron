{
  "name": "patron-desktop-root",
  "version": "0.1.0",
  "private": true,
  "description": "Root orchestrator buildu Patrona (backend + frontend + bundle MCP). Nie jest workspace - tylko skrypty wygody dla wdrozenia (IMPLEMENTATION_PLAYBOOK Tydzien 1).",
  "scripts": {
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "bundle:mcp": "node scripts/bundle-mcp.cjs",
    "build:all": "npm run build:backend && npm run build:frontend",
    "test:backend": "npm --prefix backend test",
    "smoke:desktop": "npm --prefix backend run smoke:desktop"
  }
}
